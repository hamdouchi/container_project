name: "Utilisation des environnements"
on: push
jobs:
    environnement:
        runs-on: ubuntu-latest
        env:
            JOB_ENV_VAR: "Avalaible in all steps"
        steps:
            - name: multiligne env
              run: |
                EOF=$(dd if=/dev/urandom bs=1 count=1 status=none | base64)
                echo "RANDOM_JOKE<<EOF" >> $GITHUB_ENV
                echo "Why did the programmer quit his job? Because he didn't get arrays." >> $GITHUB_ENV
                echo "" >> $GITHUB_ENV
                echo "EOF" >> $GITHUB_ENV
            - name: "Afficher les variables d'environnement de github"
              run: |
                echo "JOB_ENV_VAR=$RANDOM" >> $GITHUB_ENV
                echo "GITHUB_REPOSITORY: $GITHUB_REPOSITORY"
                echo "GITHUB_REF: $GITHUB_REF"
                echo "GITHUB_SHA: $GITHUB_SHA"
                echo "GITHUB_ACTOR: $GITHUB_ACTOR"
                echo "GITHUB_WORKFLOW: $GITHUB_WORKFLOW"
                echo "GITHUB_RUN_ID: $GITHUB_RUN_ID"
                echo "GITHUB_RUN_NUMBER: $GITHUB_RUN_NUMBER"
                echo "GITHUB_JOB: $GITHUB_JOB"
                echo "GITHUB_REF: ${{ github.ref }}"
                echo "JOB_ENV_VAR: $JOB_ENV_VAR"
                echo "RANDOM_JOKE: $RANDOM_JOKE"
            - name: Custom environment variables
              env:
                CUSTOM_ENV_VAR: "Hello, World!"
              run: |
                echo "CUSTOM_ENV_VAR: $CUSTOM_ENV_VAR"
                echo "JOB_ENV_VAR: $JOB_ENV_VAR"
            